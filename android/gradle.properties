org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
# Évite l'erreur "Starting AGP 9+, only the new DSL interface will be read"
# tant que le plugin Flutter Gradle ne supporte pas pleinement la nouvelle DSL AGP 9.
# Réf : https://docs.flutter.dev/release/breaking-changes/migrate-to-agp-9
android.newDsl=false
